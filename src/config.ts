/**
 * Временные переключатели, которые меняются передеплоем (~2 минуты).
 *
 * Рантайм-конфига в проекте пока нет, так что это честный минимум: одна
 * строчка, один деплой. Если такие переключения станут частыми, правильнее
 * завести таблицу настроек в базе и читать её на запросе.
 */

/** Заглушка «Soon» вместо приложения. Выключить — поставить false и задеплоить. */
export const SOON_SCREEN = true;

/**
 * Показывать заглушку только тем, кто пришёл из Telegram. Обычный лендинг
 * в браузере при этом работает как обычно — мы гасим мини-апп, а не сайт.
 */
export const SOON_TELEGRAM_ONLY = true;

/** С этим параметром в адресе заглушка не показывается: `?preview=1`. */
export const SOON_BYPASS_PARAM = 'preview';
